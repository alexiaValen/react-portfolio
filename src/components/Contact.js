import React from 'react';
import '../styles/Contact.css';

const styles = {
  contactStyles: {
    background: 'white',
  },
};


function Contact() {
    return (
      <header style={styles.contactStyle} className="contact">
        <h3 style={styles.contactStyle}>contact</h3>
        <p>
        ...coming soon
       </p>
      </header>
    );
  }
  
  export default Contact;